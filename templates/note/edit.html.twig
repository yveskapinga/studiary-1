{% extends 'layouts/page.twig' %}

{% block body %}
    <div class="container pt-8 flex justify-center flex-col">
        <h1 class="text-center text-2xl font-bold m-4">Edit Note</h1>

        {{ include('note/_form.html.twig', {'button_label': 'Update'}) }}
        <div class="flex justify-center">
            <a class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded m-4" href="{{ path('notes_index') }}">Back to the list</a>
        </div>

        {{ include('note/_delete_form.html.twig') }}
    </div>
{% endblock %}
